#pragma once

#include <array>
#include <numbers>
#include <cmath>

double frandom ( );
unsigned int random ( );
void sin_cos ( float* out_sin, float* out_cos, unsigned short angle );

void init_sincos ( );

void init_ogre_maths ( );